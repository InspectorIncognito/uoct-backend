---
phase: 01-critical-bug-fixes
plan: 02
type: execute
wave: 2
depends_on: [1]
files_modified:
  - backend/gtfs_rt/models.py
  - backend/gtfs_rt/factories.py
  - backend/gtfs_rt/tests/test_model.py
autonomous: true
requirements:
  - REQ-03
must_haves:
  truths:
    - "GPSPulse stores direction in the canonical direction field"
    - "Legacy direction_id reads and writes resolve to the same numeric value"
    - "Existing duplicate-direction regression coverage uses canonical field names"
  artifacts:
    - path: "backend/gtfs_rt/models.py"
      provides: "GPSPulse.direction canonical storage plus direction_id compatibility alias"
    - path: "backend/gtfs_rt/factories.py"
      provides: "Factory defaults that use direction instead of direction_id"
    - path: "backend/gtfs_rt/tests/test_model.py"
      provides: "Regression coverage for canonical naming and alias compatibility"
  key_links:
    - from: "backend/gtfs_rt/factories.py"
      to: "backend/gtfs_rt.models.GPSPulse"
      via: "factory defaults"
      pattern: "direction\s*="
    - from: "backend/gtfs_rt/tests/test_model.py"
      to: "GPSPulse.direction_id"
      via: "compatibility assertion"
      pattern: "direction_id"
    - from: "backend/gtfs_rt.models.GPSPulse"
      to: "stored direction column"
      via: "property alias"
      pattern: "def direction_id"
---

<objective>
Normalize GPS pulse direction naming without breaking legacy `direction_id` reads.

Purpose: Remove the model/test mismatch that hides regressions while keeping older code paths functional.
Output: Updated factory defaults, canonical test fixtures, and a compatibility alias on `GPSPulse`.
</objective>

<execution_context>
@/home/shab/Documents/DCC/MDS/repos/uoct-backend/.opencode/get-shit-done/workflows/execute-plan.md
@/home/shab/Documents/DCC/MDS/repos/uoct-backend/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-critical-bug-fixes/01-CONTEXT.md

<interfaces>
From `backend/gtfs_rt/models.py`:
```python
class GPSPulse(models.Model):
    direction = models.IntegerField(null=True, blank=True)
```

From `backend/gtfs_rt/tests/test_model.py`:
```python
class GPSPulsesTest(BaseTestCase):
    def create_gps_instances(self):
        ...
```

From `backend/gtfs_rt/factories.py`:
```python
class GPSPulseFactory(factory.django.DjangoModelFactory):
    class Meta:
        model = GPSPulse
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redesign the GPSPulse regression tests around direction as the canonical field</name>
  <files>backend/gtfs_rt/factories.py, backend/gtfs_rt/tests/test_model.py</files>
  <read_first>
    - backend/gtfs_rt/models.py
    - backend/gtfs_rt/factories.py
    - backend/gtfs_rt/tests/test_model.py
  </read_first>
  <behavior>
    - Test fixtures must pass `direction`, not `direction_id`, when creating `GPSPulse` rows.
    - The distinct/count coverage must query `direction` so the canonical column is exercised.
    - Regression coverage must still prove that legacy `direction_id` access returns the same numeric value on an instance.
  </behavior>
  <action>Rewrite the GPSPulse regression tests so the fixture dictionary uses `direction`, the distinct query keys use `direction`, and the test suite asserts the legacy alias still mirrors the stored numeric value. Give the factory sensible defaults for the canonical GPSPulse fields so future tests can instantiate it without repeating boilerplate.</action>
  <acceptance_criteria>
    - `backend/gtfs_rt/tests/test_model.py` no longer uses `direction_id` in fixture dictionaries for creating `GPSPulse` rows.
    - `backend/gtfs_rt/tests/test_model.py` queries `direction` in the distinct coverage.
    - `backend/gtfs_rt/factories.py` exposes a canonical `direction` default.
  </acceptance_criteria>
  <verify>
    <automated>python backend/manage.py test gtfs_rt.tests.test_model -v 2</automated>
  </verify>
  <done>The test file and factory reflect canonical field naming and still encode the legacy compatibility expectation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a backward-compatible direction_id alias on GPSPulse</name>
  <files>backend/gtfs_rt/models.py</files>
  <read_first>
    - backend/gtfs_rt/models.py
    - backend/gtfs_rt/tests/test_model.py
    - backend/gtfs_rt/factories.py
  </read_first>
  <behavior>
    - `GPSPulse.direction` remains the stored canonical field.
    - `GPSPulse.direction_id` resolves to the same backing value for reads and writes.
    - No database schema change is required for the direction-field rename.
  </behavior>
  <action>Add a small compatibility shim on `GPSPulse` that exposes `direction_id` as an alias for `direction` while leaving the model field name unchanged. Keep the implementation minimal and local to the model class so existing readers continue to work without creating a second persisted column.</action>
  <acceptance_criteria>
    - `backend/gtfs_rt/models.py` defines `GPSPulse.direction_id` as a property or equivalent alias.
    - Setting `pulse.direction_id` updates `pulse.direction`.
    - Reading `pulse.direction_id` returns the same numeric value as `pulse.direction`.
  </acceptance_criteria>
  <verify>
    <automated>python backend/manage.py test gtfs_rt.tests.test_model -v 2</automated>
  </verify>
  <done>Direction naming is normalized without breaking legacy access patterns, and the regression suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture data → Django ORM | Stale field names can create false confidence or break record creation |
| Model API → downstream readers | Legacy code may still read `direction_id` even after `direction` becomes canonical |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | T | backend/gtfs_rt/models.py | mitigate | Keep `direction` as the stored field and expose `direction_id` as a compatibility alias so reads and writes stay consistent. |
| T-01-04 | D | backend/gtfs_rt/factories.py, backend/gtfs_rt/tests/test_model.py | mitigate | Update factory defaults and regression fixtures to use `direction`, preventing stale test data from masking model regressions. |
</threat_model>

<verification>
[Overall phase checks]
- `python backend/manage.py test gtfs_rt.tests.test_model -v 2` passes.
- `GPSPulse.direction` is the only persisted direction field.
- `direction_id` access still works through the compatibility shim.
</verification>

<success_criteria>
- The model, factory, and tests all agree on `direction` as the canonical field name.
- Legacy `direction_id` reads and writes keep working through the model alias.
- Phase 1 requirements remain fully covered with no dropped regression behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-critical-bug-fixes/01-02-SUMMARY.md`
</output>
