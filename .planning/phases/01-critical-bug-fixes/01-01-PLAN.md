---
phase: 01-critical-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/gtfs_rt/tests/test_manager.py
  - backend/gtfs_rt/processors/manager.py
  - backend/gtfs_rt/models.py
  - backend/gtfs_rt/migrations/0012_alter_gtfsrttimestamp_last_timestamp.py
autonomous: true
requirements:
  - REQ-01
  - REQ-02
  - REQ-12
must_haves:
  truths:
    - "GTFSRTManager does not crash when GTFSRTTimestamp is empty"
    - "GTFS-RT duplicate feed timestamps are compared numerically, not as strings"
    - "last_timestamp is updated after a successful feed save"
  artifacts:
    - path: "backend/gtfs_rt/tests/test_manager.py"
      provides: "Regression coverage for empty-table startup and duplicate timestamp handling"
    - path: "backend/gtfs_rt/processors/manager.py"
      provides: "Self-seeding save logic and numeric timestamp comparison"
    - path: "backend/gtfs_rt/models.py"
      provides: "Integer checkpoint storage for GTFSRTTimestamp"
    - path: "backend/gtfs_rt/migrations/0012_alter_gtfsrttimestamp_last_timestamp.py"
      provides: "Database migration for integer checkpoint storage"
  key_links:
    - from: "backend/gtfs_rt/processors/manager.py"
      to: "GTFSRTTimestamp.objects.get_or_create()"
      via: "save_gtfs_rt_to_db bootstrap path"
      pattern: "get_or_create\(.*last_timestamp"
    - from: "backend/gtfs_rt/processors/manager.py"
      to: "GPSPulse.objects.create()"
      via: "feed entity write loop"
      pattern: "GPSPulse\.objects\.create"
    - from: "backend/gtfs_rt/models.py"
      to: "backend/gtfs_rt/migrations/0012_alter_gtfsrttimestamp_last_timestamp.py"
      via: "model field change"
      pattern: "last_timestamp.*Integer|BigInteger"
---

<objective>
Prevent GTFS-RT ingestion crashes and silent duplicate writes by making the timestamp checkpoint self-seeding and numeric.

Purpose: Fix correctness before any profiling or optimization so later measurements are trustworthy.
Output: Red regression tests, safer manager logic, integer checkpoint storage, and a migration that matches the model.
</objective>

<execution_context>
@/home/shab/Documents/DCC/MDS/repos/uoct-backend/.opencode/get-shit-done/workflows/execute-plan.md
@/home/shab/Documents/DCC/MDS/repos/uoct-backend/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-critical-bug-fixes/01-CONTEXT.md

<interfaces>
From `backend/gtfs_rt/processors/manager.py`:
```python
class GTFSRTManager:
    def save_gtfs_rt_to_db(self, feed: gtfs_realtime_pb2.FeedMessage):
        ...
```

From `backend/gtfs_rt/models.py`:
```python
class GTFSRTTimestamp(SingletonModel):
    last_timestamp = models.CharField(default="", max_length=124)
```

From `backend/gtfs_rt/models.py`:
```python
class GPSPulse(models.Model):
    direction = models.IntegerField(null=True, blank=True)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add red regression coverage for empty checkpoint rows and numeric duplicate detection</name>
  <files>backend/gtfs_rt/tests/test_manager.py</files>
  <read_first>
    - backend/gtfs_rt/processors/manager.py
    - backend/gtfs_rt/models.py
    - backend/gtfs_rt/tests/test_model.py
  </read_first>
  <behavior>
    - Empty `GTFSRTTimestamp` must be auto-created instead of crashing on `objects.first()`.
    - Feed timestamps must compare as integers so equal-or-older feed data is rejected and newer data is written.
    - Successful writes must update `last_timestamp` after all `GPSPulse` rows are created.
  </behavior>
  <action>Create a new manager regression test module that uses mocked GTFS-RT feeds and a clean database state to prove the three behaviors above. Keep the tests focused on `GTFSRTManager.save_gtfs_rt_to_db()` and avoid touching production code in this task.</action>
  <acceptance_criteria>
    - `backend/gtfs_rt/tests/test_manager.py` defines a test for an empty `GTFSRTTimestamp` table.
    - `backend/gtfs_rt/tests/test_manager.py` defines a test that rejects equal or older numeric timestamps.
    - `backend/gtfs_rt/tests/test_manager.py` defines a test that confirms `last_timestamp` advances after a successful save.
  </acceptance_criteria>
  <verify>
    <automated>python backend/manage.py test gtfs_rt.tests.test_manager -v 2</automated>
  </verify>
  <done>The regression suite is in place and fails red until the manager/model fix lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make GTFSRTTimestamp self-seeding and store timestamps as integers</name>
  <files>backend/gtfs_rt/processors/manager.py, backend/gtfs_rt/models.py, backend/gtfs_rt/migrations/0012_alter_gtfsrttimestamp_last_timestamp.py</files>
  <read_first>
    - backend/gtfs_rt/processors/manager.py
    - backend/gtfs_rt/models.py
    - backend/gtfs_rt/migrations/0011_gpspulse_gtfs_rt_gps_timesta_52d348_idx.py
    - backend/gtfs_rt/tests/test_manager.py
  </read_first>
  <behavior>
    - `GTFSRTTimestamp.last_timestamp` is stored as a numeric field, not a string field.
    - `GTFSRTManager.previous_timestamp` is initialized and compared as an integer.
    - `save_gtfs_rt_to_db()` uses `GTFSRTTimestamp.objects.get_or_create(pk=1, defaults={"last_timestamp": 0})` and only persists the checkpoint after the feed write succeeds.
  </behavior>
  <action>Refactor the manager to coerce feed timestamps to integers, create the singleton checkpoint row when missing, compare timestamps numerically, and update the checkpoint only after the write loop succeeds. Add the matching migration that converts `GTFSRTTimestamp.last_timestamp` from `CharField` to an integer-backed field.</action>
  <acceptance_criteria>
    - `backend/gtfs_rt/processors/manager.py` contains `get_or_create(pk=1, defaults={"last_timestamp": 0})`.
    - `backend/gtfs_rt/processors/manager.py` no longer compares timestamps as strings.
    - `backend/gtfs_rt/models.py` defines `GTFSRTTimestamp.last_timestamp` as an integer-backed field.
    - `backend/gtfs_rt/migrations/0012_alter_gtfsrttimestamp_last_timestamp.py` alters the column to the same integer storage.
  </acceptance_criteria>
  <verify>
    <automated>python backend/manage.py test gtfs_rt.tests.test_manager -v 2</automated>
  </verify>
  <done>Clean-start ingestion works, duplicate feed suppression is numeric, and the checkpoint survives restarts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GTFS-RT feed → parser → DB writes | Untrusted feed payloads and timestamps cross into persistence logic |
| Django model state → worker process | A missing singleton row or stale checkpoint can crash or corrupt ingestion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | D | backend/gtfs_rt/processors/manager.py | mitigate | Seed `GTFSRTTimestamp` with `get_or_create()` and treat missing state as normal startup, not an exception path. |
| T-01-02 | T | backend/gtfs_rt/processors/manager.py, backend/gtfs_rt/models.py | mitigate | Store and compare Unix epoch timestamps as integers, then persist the checkpoint only after a successful write loop. |
</threat_model>

<verification>
[Overall phase checks]
- `python backend/manage.py test gtfs_rt.tests.test_manager -v 2` passes after Task 2.
- `python backend/manage.py test gtfs_rt.tests.test_model -v 2` still passes after the shared model change.
- The migration file exists and matches the integer checkpoint field.
</verification>

<success_criteria>
- `GTFSRTManager.save_gtfs_rt_to_db()` no longer crashes on a clean `GTFSRTTimestamp` table.
- Duplicate GTFS-RT feeds are suppressed by numeric timestamp comparison, not string ordering.
- `GTFSRTTimestamp.last_timestamp` is stored as an integer-backed field with a matching migration.
</success_criteria>

<output>
After completion, create `.planning/phases/01-critical-bug-fixes/01-01-SUMMARY.md`
</output>
